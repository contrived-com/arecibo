# Claudia Progress Log

This file tracks completed work by the Claudia autonomous agent.
Each entry documents what was implemented and lessons learned.

## Codebase Patterns

- Rust toolchain: use `RUSTUP_HOME=$HOME/.rustup CARGO_HOME=$HOME/.cargo` in CI containers where `/usr/local/cargo` is read-only
- Transponder binary name: `transponder` (declared in Cargo.toml [[bin]])
- HTTP client: uses `ureq` 2.x (blocking, lightweight) instead of `reqwest` to avoid async/tokio overhead
- Thread-safe counters: `Arc<Mutex<TransponderCounters>>` shared between runtime and ingest threads; locks are never held during HTTP calls
- Ingest socket: background thread with 500ms read timeout for clean shutdown (matches Python socket.close() pattern)
- Config env parsing: `env_bool` for truthy values, `env_int` with minimum enforcement, float timeout parsing with defaults
- Vault fallback: synchronous ureq calls with 2s timeout, only runs once at startup
- Build script: `build.rs` captures `RUSTC_VERSION` at compile time for announce payload
- Announce payload uses `rustVersion` instead of `pythonVersion`
- All JSON timestamps use RFC 3339 UTC with trailing Z
- Dockerfile.transponder: multi-stage (rust:1.85-bookworm builder → debian:bookworm-slim runtime); dummy main.rs trick caches deps layer
- Entrypoint default: `/opt/transponder/bin/transponder` (was `.venv/bin/transponder` in Python era)
- Test suite: 39 tests across all modules; `cargo test` runs without external dependencies
- API tests: run with `VAULT_ADDR= python -m pytest api/tests/ -v` (unset Vault vars to use local dev keys)
- Telemetry storage layout: `data/telemetry/{YYYY-MM-DD}/{serviceName}/{environment}/{type}.jsonl`
- Telemetry store writes are fire-and-forget (log errors, don't block response)
- Retention walks date directories, prunes based on `ARECIBO_RETENTION_DAYS` (default 180)
- Query API follows Grafana JSON datasource conventions: GET with query params, not POST
- Schema pattern: schemas versioned as `{type}.1.0.0.json` with `$id` for naming, `$ref` for composition

---

## 2026-03-03: arecibo-00001 - Initialize Rust project structure

### Summary
Removed Python packaging layout (pyproject.toml, uv.lock, transponder.py, src/arecibo_transponder/, tests/) and created Rust project skeleton with Cargo.toml, build.rs, and module files.

### Files Changed
- transponder/Cargo.toml (new)
- transponder/build.rs (new)
- transponder/src/main.rs (new)
- transponder/src/config.rs (new)
- transponder/src/client.rs (new)
- transponder/src/model.rs (new)
- transponder/src/ingest.rs (new)
- transponder/src/runtime.rs (new)
- transponder/src/utils.rs (new)
- .gitignore (added transponder/target/)
- Removed: pyproject.toml, uv.lock, transponder.py, src/arecibo_transponder/*, tests/*

### Learnings
- System rustc 1.81 was too old for latest crate versions (getrandom 0.4 needs edition2024). Installed Rust 1.93.1 via rustup to home dir.

---

## 2026-03-03: arecibo-00002 - Config/env parsing with Vault fallback

### Summary
Implemented TransponderConfig struct with from_env() that reads all TRANSPONDER_* env vars with identical defaults and precedence as Python. Vault AppRole login and secret read for API key fallback preserved with ureq blocking HTTP.

### Files Changed
- transponder/src/config.rs

### Learnings
- env_bool matches Python truthiness: "1", "true", "yes", "on" (case-insensitive)
- Collector candidate dedup preserves insertion order (override first, then defaults)

---

## 2026-03-03: arecibo-00003 - HTTP collector client and model types

### Summary
Implemented CollectorClient with health/announce/policy/heartbeat/events_batch methods using ureq. Error handling preserves Python semantic parity: HTTP errors return (code, parsed_body), connection errors return (0, None). Model types (Directive, PolicyState, TransponderCounters) ported to Rust.

### Files Changed
- transponder/src/client.rs
- transponder/src/model.rs

### Learnings
- Manual URL encoding for query params avoids extra dependency; only serviceName/environment used
- ureq Error::Status variant maps cleanly to Python HTTPError pattern

---

## 2026-03-03: arecibo-00004 - Unix datagram ingest and bounded queue

### Summary
Implemented IngestQueue with Mutex-protected VecDeque, drop-oldest overflow with counter updates. IngestDatagramServer runs in background thread with 500ms read timeout, parses JSON datagrams, normalizes events (ts/type/severity/payload/tags).

### Files Changed
- transponder/src/ingest.rs

### Learnings
- Split lock acquisition in push() to avoid holding both queue and counter locks simultaneously (prevents deadlock)
- Socket read timeout instead of closing from another thread is cleaner for Rust ownership model

---

## 2026-03-03: arecibo-00005 - Runtime loop with lifecycle and shutdown

### Summary
Implemented TransponderRuntime with bootstrap (collector probing, announce, policy fetch), main loop (heartbeat/policy refresh/event flush on intervals, 200ms tick), directive handling (GO_DARK/RESUME/REFRESH_POLICY/SET_HEARTBEAT_INTERVAL/FLUSH_STATS), and graceful SIGTERM/SIGINT shutdown via ctrlc crate.

### Files Changed
- transponder/src/runtime.rs
- transponder/src/main.rs

### Learnings
- Counter lock must be dropped before HTTP calls and before apply_directives (which may call refresh_policy)
- Re-queue on failed event batch preserves Python behavior (events go back through push() which increments received counter)
- ctrlc crate handles both SIGTERM and SIGINT with a single handler via AtomicBool

---

## 2026-03-03: arecibo-00006 - Update Dockerfile.transponder and entrypoint.sh

### Summary
Rewrote Dockerfile.transponder as a multi-stage Rust build: builder stage with `rust:1.85-bookworm` compiles the binary, runtime stage with `debian:bookworm-slim` copies only the binary to `/opt/transponder/bin/transponder`. Updated entrypoint.sh default TRANSPONDER_BIN path. Removed all Python-specific artifacts (Python base image, PYTHONDONTWRITEBYTECODE, uv/pip).

### Files Changed
- Dockerfile.transponder
- transponder/entrypoint.sh

### Learnings
- Dummy main.rs trick in Dockerfile caches dependency compilation in a separate layer
- ca-certificates needed in runtime image for TLS (ureq uses rustls + webpki-roots but system certs still useful)

---

## 2026-03-03: arecibo-00007 - Port and write Rust transponder tests

### Summary
Added 39 unit tests across config, model, ingest, client, and runtime modules. Tests cover env_bool/env_int parsing, collector candidate dedup, counter reset, queue overflow with counter updates, ingest socket lifecycle with event normalization, directive parsing, GO_DARK/RESUME toggling, SET_HEARTBEAT_INTERVAL with minimum enforcement, policy-disabled event drops, and HTTP connection failure handling. All tests pass without external dependencies.

### Files Changed
- transponder/src/config.rs (added #[cfg(test)] module, made env_bool/env_int pub(crate))
- transponder/src/model.rs (added #[cfg(test)] module)
- transponder/src/ingest.rs (added #[cfg(test)] module)
- transponder/src/runtime.rs (added #[cfg(test)] module)
- transponder/src/client.rs (added #[cfg(test)] module)

### Learnings
- Use unique env var names per test to avoid parallel test interference
- Process-local Unix sockets in /tmp work well for ingest server integration tests
- No mock framework needed; manual test doubles and direct struct construction suffice

---

## 2026-03-03: arecibo-00008 - Update documentation and logging artifacts

### Summary
Updated AGENTS.md to reference Cargo.toml and Rust binary artifact. Rewrote transponder/README.md for Rust development workflow. Created scripts/claudia/rust-tooling-feedback.md with crate recommendations, build time observations, musl considerations, and CI caching advice.

### Files Changed
- AGENTS.md
- transponder/README.md
- scripts/claudia/progress.txt
- scripts/claudia/rust-tooling-feedback.md (new)

---

## 2026-03-03: arecibo-00009 - Define observability query API in OpenAPI spec

### Summary
Extended openapi.yml with 5 query endpoints under new 'observability' tag: fleet-health, heartbeat-freshness, event-throughput, go-dark-status, and recent-events. Created corresponding JSON schemas under schemas/query/. Added reusable query parameter components (start, end, maxRows, cursor, serviceName, environment filters).

### Files Changed
- openapi.yml (added query endpoints and parameter components)
- schemas/query/fleet-health.1.0.0.json (new)
- schemas/query/heartbeat-freshness.1.0.0.json (new)
- schemas/query/event-throughput.1.0.0.json (new)
- schemas/query/go-dark-status.1.0.0.json (new)
- schemas/query/recent-events.1.0.0.json (new)

### Learnings
- Query endpoints use GET with query params for Grafana JSON datasource compatibility
- Reusable $ref parameter components keep the spec DRY

---

## 2026-03-03: arecibo-00010 - Implement telemetry file storage module

### Summary
Created TelemetryStore class that writes ingest payloads to data/telemetry/ in JSONL files partitioned by date/service/environment. Wired into app.py lifespan and all three ingest endpoints (announce, heartbeat, events:batch). Writes are fire-and-forget to avoid blocking responses.

### Files Changed
- api/src/telemetry_store.py (new)
- api/src/app.py (added TelemetryStore initialization and calls)
- .gitignore (added data/telemetry/** exclusion)
- data/telemetry/.gitkeep (new)

### Learnings
- Directory name sanitization prevents path traversal from serviceName/environment values
- Append-mode writes with single JSON line per record keeps files safe for concurrent access

---

## 2026-03-03: arecibo-00011 - Implement 180-day telemetry retention

### Summary
Created retention module that prunes date-partitioned directories older than configurable retention period (default 180 days via ARECIBO_RETENTION_DAYS). Supports dry-run mode and deterministic execution with injectable clock. Wired into app.py lifespan via run_in_executor for non-blocking startup.

### Files Changed
- api/src/telemetry_retention.py (new)
- api/src/app.py (added retention import and background startup task)

### Learnings
- shutil.rmtree for date partition cleanup is safe since partitions are self-contained
- run_in_executor with lambda avoids keyword arg limitations

---

## 2026-03-03: arecibo-00014 - Add Arecibo to contrived-site APP_REGISTRY

### Summary
Verified that Arecibo is already fully integrated in contrived-site: APP_REGISTRY entry with viewer/admin roles, Stytch RBAC access computation, session payload inclusion, admin member listing, dashboard card rendering with locked/unlocked state, and proxy setup. No changes needed.

### Files Changed
- (none - already implemented in contrived-site)

---
